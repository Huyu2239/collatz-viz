# CollatzViz

コラッツ予想の逆木を可視化するやつ。

1をルートにして、大きい数に向かって枝が伸びていく木構造を2Dで描画する。2の累乗が幹になって、そこから枝が右に伸びる感じ。

## 使い方

```sh
bun install
bun run dev
```

左上のスライダーで木のサイズ（最大数）を変えられる。ドラッグでパン、スクロールでズーム。

## ビルド

```sh
bun run build
```

`dist/` に静的ファイルが生成される。

## ライセンス

MIT
